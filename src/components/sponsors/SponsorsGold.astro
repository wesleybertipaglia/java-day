---
import { sponsors } from "../../data/partners";
import Link from "../Link.astro";

const gold = sponsors.filter(
    (sponsor) => sponsor.type.toLowerCase() === "gold",
);
---

<div class="flex flex-col gap-4">
    <h3>Ouro</h3>

    <div
        class="grid grid-cols-2 md:grid-cols-3 gap-6 justify-center items-center border border-secundary-light"
    >
        {
            gold.map(({ url, name, logo, invert }) => (
                <Link href={url} target="_blank">
                    <figure
                        class="h-auto w-full aspect-video flex items-center justify-center p-6"
                        title={name}
                    >
                        <img
                            src={logo}
                            alt={name}
                            class="h-56 w-56 object-contain"
                            class:list={
                                invert ? "filter brightness-0 invert" : ""
                            }
                        />
                    </figure>
                </Link>
            ))
        }
    </div>
</div>
